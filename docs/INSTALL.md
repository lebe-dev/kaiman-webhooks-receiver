# Installation

```bash
mkdir /opt/kwp
cd /opt/kwp

cp .env-example /opt/kwp/.env
cp config.yml-dist /opt/kwp/config.yml

# edit .env and config.yml for your needs

docker compose up -d
```

## What's next

- [Integration guides](INTEGRATIONS.md)
